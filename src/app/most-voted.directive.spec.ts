import { MostVotedDirective } from './most-voted.directive';

describe('MostVotedDirective', () => {
  it('should create an instance', () => {
    const directive = new MostVotedDirective();
    expect(directive).toBeTruthy();
  });
});
